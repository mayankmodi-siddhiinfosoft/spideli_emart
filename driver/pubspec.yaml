name: driver
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.2+6

environment:
  sdk: ^3.13.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.9
  firebase_core: ^4.15.0
  firebase_auth: ^6.7.0
  cloud_firestore: ^6.10.0
  firebase_database: ^12.6.0
  firebase_storage: ^13.6.0
  firebase_messaging: ^16.7.0
  firebase_app_check: ^0.4.8
  googleapis_auth: ^2.0.0
  country_code_picker: ^3.4.1
  crypto: ^3.0.7

  dotted_border: ^3.1.0
  geocoding: ^5.0.0

  cached_network_image: ^4.0.0
  flutter_easyloading: ^4.0.2
  flutter_email_sender: ^10.0.1
  flutter_html: any
  flutter_local_notifications: ^22.3.1
  flutter_svg: ^2.2.4
  shimmer: ^4.0.0
  location: ^10.0.2
  get: ^4.7.3
  google_maps_flutter: ^2.16.0
  #  google_sign_in: ^6.2.2
  google_sign_in: ^7.2.0

  image_picker: ^1.2.1
  intl: ^0.20.2
  # Held on 9.x: 10.x reads material_ui's Theme (app uses SDK material), so unset MaterialPinTheme colors/text fall back to default palette - changelog says stay on ^9.4.0.
  pin_code_fields: ^9.4.0

  provider: ^6.1.5+1
  qr_flutter: ^4.1.0
  shared_preferences: ^2.5.5
  sign_in_with_apple: ^8.2.0
  syncfusion_flutter_datepicker: ^34.2.8
  timer_count_down: ^2.2.2
  url_launcher: ^6.3.2
  uuid: ^4.5.3
  date_picker_timeline: ^1.2.7
  flutter_rating_bar: ^4.0.1
  flutter_polyline_points: ^3.1.0
  map_launcher: ^6.0.0
  timelines_plus: ^2.0.0
  in_app_review: ^2.0.11
  share_plus: ^13.3.0
  mailer: ^7.1.0
  photo_view: ^0.15.0
  path_provider: ^2.1.5
  path: ^1.9.1
  video_compress: ^3.1.4
  video_player: ^2.11.1
  audioplayers: ^6.6.0

  flutter_map: ^8.2.2
  latlong2: ^0.10.1

  #payment dependency
  flutter_stripe: ^14.0.0
  razorpay_flutter: ^1.4.3
  flutter_paypal: ^0.2.1
  geolocator: ^14.0.3
  flutter_google_places_hoc081098: ^2.0.0
  mobile_scanner: ^7.4.2

dependency_overrides:
  # flutter_html 3.0.0 (latest) calls html's private top-level `matches()`, which
  # html 0.15.7 removed -- a compile error the analyzer does not report. 0.15.6 is
  # the version the app used before this upgrade.
  html: 0.15.6
  webview_flutter: ^4.9.0
  http: ^1.6.0
  # flutter_google_places_hoc081098 2.0.0 (latest) pins google_api_headers ^4 -> package_info_plus ^8 -> win32 <6,
  # which blocks share_plus 13 / geolocator 14.0.3. The only API it uses, `const GoogleApiHeaders().getHeaders()`,
  # is unchanged in 5.x (5.0.0 was a native-toolchain bump only).
  # Pinned to 5.4.0: 5.4.1 declares sdk >=3.13.4 / flutter >=3.47.5 (overrides skip that check); relax once on Flutter 3.47.5+.
  google_api_headers: 5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  fonts:
    - family: Urbanist-Black
      fonts:
        - asset: assets/fonts/Urbanist-Black.ttf

    - family: Urbanist-Bold
      fonts:
        - asset: assets/fonts/Urbanist-Bold.ttf

    - family: Urbanist-ExtraBold
      fonts:
        - asset: assets/fonts/Urbanist-ExtraBold.ttf

    - family: Urbanist-ExtraLight
      fonts:
        - asset: assets/fonts/Urbanist-ExtraLight.ttf

    - family: Urbanist-Light
      fonts:
        - asset: assets/fonts/Urbanist-Light.ttf

    - family: Urbanist-Medium
      fonts:
        - asset: assets/fonts/Urbanist-Medium.ttf

    - family: Urbanist-Regular
      fonts:
        - asset: assets/fonts/Urbanist-Regular.ttf

    - family: Urbanist-SemiBold
      fonts:
        - asset: assets/fonts/Urbanist-SemiBold.ttf

    - family: Urbanist-Thin
      fonts:
        - asset: assets/fonts/Urbanist-Thin.ttf

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/
    - assets/fonts/
    - assets/audio/
    - assets/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
